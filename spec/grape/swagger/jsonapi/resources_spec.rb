RSpec.describe Grape::Swagger::Jsonapi::Resources do
  it "has a version number" do
    expect(Grape::Swagger::Jsonapi::Resources::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
